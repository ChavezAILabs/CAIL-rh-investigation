# RH Phase 18C Handoff — AIEX-001 Operator Construction

**Researcher:** Paul Chavez, Chavez AI Labs LLC
**Date:** March 16, 2026
**Status:** Ready to execute
**Depends on:** Phases 16, 17, 18A, 18B, 18E (complete)

---

## Background

Phases 16–18B have established the following empirical and algebraic foundation:

- P-vectors and Q-vectors lie on the E8 first shell and form a single Weyl orbit; they span 4D and 6D subspaces respectively (Phases 17, 18E)
- The complete bilateral root set has (A₁)⁶ structure — 6 mutually orthogonal ±pairs in a 6D subspace of E8 (Phase 18E)
- The Bilateral Collapse Theorem forces the sedenion product x_n·x_{n+1} entirely into the scalar channel for Pattern 1 (Phase 18B)
- The functional equation bilateral pairing ρ ↔ 1−ρ̄ has a candidate algebraic counterpart in v2+v3=0 (Theorem_1b, Lean 4 verified, zero sorry stubs)
- **Theorem_1b establishes both v2+v3=0 AND s_α4(v2)=v3** — the antipodal constraint is not an algebraic coincidence; it is generated by a specific simple reflection in the E8 Weyl group
- The chi3/Q2 anomaly (mean ratio 1.165, Phase 18A) is conductor-specific to conductor 3 and unexplained

Phase 18C uses this foundation to sharpen the AIEX-001 conjecture into a candidate operator statement.

---

## Research Questions

### Q5 — Height Monotonicity Normalization Fix (dispose first)

Re-run Phase 18B-iii with **per-window empirical mean gap** for synthetic GUE/Poisson generation. For each height window, use the mean of actual gaps within that window as the scale parameter — not the global mean (0.9865).

**Primary question:** Does the monotonic drop in actual three-gap variance survive empirical normalization?

If yes: Riemann zeros become more regular than GUE at the three-gap scale at large height — a specific, strong result that feeds directly into how H should behave asymptotically.

If no: Phase 18B-iii is cleanly closed as a normalization artifact.

Either outcome is useful. This runs first and the result is in hand before Q1–Q4 begin.

**Expected effect:** GUE variance will drop across windows (matching the gap⁴ scaling with height), narrowing the 20× Act/GUE range seen in Phase 18B. The question is whether the actual variance drops *faster* than locally-normalized GUE.

---

### Q2 — P/Q Split in the Explicit Formula

The explicit formula:

> ψ(x) = x − Σ_ρ x^ρ/ρ − log(2π) − ½log(1−x⁻²)

Zeros ρ appear as oscillatory terms indexed by primes. Phase 17A and 18B established that P-vector projections show sharp SNR peaks at specific primes (narrow-band: p=7..23 dominant) while Q-vector projections show broadband SNR response across the full prime spectrum including p=2.

**Primary question:** Does the P/Q SNR profile match a natural narrow/broadband decomposition of the explicit formula's prime sum?

Specifically: do P-vector projections selectively pick up short prime orbits (large log p, high frequency) while Q-vector projections respond to long/broadband structure (small log p, low frequency, including p=2)?

**Operational test:** Tabulate the log-prime DFT SNR profiles for all P and Q projections side by side. A systematic pattern — P high-pass, Q low-pass/broadband — confirms Q1 as a corollary and establishes the filter bank interpretation on an empirical basis. Data already exists from Phases 13A, 14B, 15D, 17A; this is synthesis, not new computation.

---

### Q1 — Filter Bank Interpretation (corollary of Q2)

If Q2 confirms the P/Q high-pass/broadband split, state explicitly as a named corollary:

> **Corollary (Filter Bank):** The embed_pair kernel decomposes prime spectral content into two complementary channels: P-projection = narrow-band filter over short prime orbits (p≥7); Q-projection = broadband filter covering the full prime spectrum (p=2..23). Together they span the complete Euler product structure detected in Phases 13–17.

Document as a named result, not a separate experiment. If Q2 is inconclusive, Q1 defers to 18D.

---

### Q3 — Bilateral Constraint Correspondence (core of 18C)

**The sharpened question (from Theorem_1b):**

> Does the functional equation symmetry s → 1−s in the critical strip correspond to the Weyl reflection s_α4 in the E8 root system?

**What is established:**

- **Algebraic side (Lean 4, zero sorry stubs):** `Theorem_1b : v2 + v3 = 0 ∧ s α4 v2 = v3` — the antipodal pair v2+v3=0 is generated by the simple Weyl reflection s_α4 acting on v2. This is not merely a sum-to-zero condition; it is the orbit of v2 under a specific element of W(E8).
- **Analytic side (classical):** ξ(s) = ξ(1−s) pairs every nontrivial zero ρ with its mirror 1−ρ̄. The functional equation is a bilateral pairing on the critical strip.

**The candidate correspondence:**

> s → 1−s  ≅  s_α4 acting on the sedenion zero divisor structure

If this correspondence holds, the functional equation symmetry is not merely analogous to the Weyl reflection — it *is* that reflection, expressed in the language of L-function arithmetic rather than root system geometry. The critical line Re(s)=½ would then be the fixed-point set of s_α4, just as the midpoint of v2+v3 is the origin.

**Q3's task:** Construct the candidate map explicitly and check whether it respects both bilateral structures simultaneously. The map need not be proven this phase; it needs to be stated precisely enough to be falsifiable. Specifically:

1. Write down the proposed dictionary: ρ ↔ v2, 1−ρ̄ ↔ v3, s→1−s ↔ s_α4
2. Check whether the fixed-point condition is consistent: s=½ ↔ fixed point of s_α4 (which would require s_α4 fixes a half-dimensional subspace — verify)
3. Identify what would need to be true for the correspondence to extend from Pattern 1/v2/v3 to the full Canonical Six

**Lean 4 target:** `aiex001_functional_equation_correspondence` — formally state the proposed dictionary as a definition; lemmas that check it are Phase 19+ work.

---

### Q4 — chi3/Q2 Anomaly Structure

**Layer 1 (runs in parallel, no Q3 dependency):**

CAILculator comparison of chi3 Q2 sequence vs zeta Q2 sequence:

- Input: `p18c_chi3_q2_sequence.json` (1,891 values, mean ≈ 0.4995) vs `p18c_zeta_q2_sequence_full.json` (9,998 values, mean ≈ 0.4896)
- Run: Chavez Transform, pattern detection, SNR profiles on both sequences
- Standard parameters: Pattern 1, α=1.0, dimension parameter=2, dimensions 1–5
- Key metric: Does CAILculator distinguish the two sequences? What is the transform magnitude ratio? Does conjugation symmetry differ?

**Layer 2 (gated on Q3):**

If Q3 produces a coherent bilateral correspondence map, interpret the chi3/Q2 ≈ 1.0 anomaly in representation-theoretic terms: is the structural alignment between q2=(e5+e10) and conductor-3 arithmetic expressible as a property of the s_α4 correspondence? Specifically — does α4 (the simple root underlying the Weyl reflection) have a natural relationship to conductor 3 in the L-function zoo?

If Q3 is inconclusive this phase, Layer 2 defers to 18D with Layer 1 empirical results in hand.

---

## Data Files

| File | Status | Generated from |
|---|---|---|
| `rh_gaps_10k.json` | ✓ exists | — |
| `zeros_chi3_2k.json` | ✓ exists | — |
| `p18c_zeta_q2_sequence_full.json` | generate at script start | `rh_zeros_10k.json` → gaps → embed_pair projected onto q2 = (0,0,−1,0,0,+1,0,0), all 9,998 pairs |
| `p18c_chi3_q2_sequence.json` | generate at script start | `zeros_chi3_2k.json` → gaps → embed_pair projected onto q2 = (0,0,−1,0,0,+1,0,0), all 1,891 pairs |

**Verification before proceeding:** chi3 Q2 mean ≈ 0.4995, zeta Q2 mean ≈ 0.4896.

---

## Execution Order

1. Generate `p18c_zeta_q2_sequence_full.json` and `p18c_chi3_q2_sequence.json` — verify means
2. **Q5** — per-window normalization fix, dispose cleanly
3. **Q4 Layer 1** — CAILculator on chi3 Q2 vs zeta Q2 (parallel, no dependencies)
4. **Q2** — P/Q split synthesis from existing SNR data (Phases 13A, 14B, 15D, 17A)
5. **Q1** — state filter bank corollary if Q2 confirms
6. **Q3** — bilateral constraint correspondence, candidate map construction
7. **Q4 Layer 2** — if Q3 conclusive, representation-theoretic interpretation of chi3/Q2
8. **Synthesis** — sharpened AIEX-001 candidate statement

---

## Expected Outcomes

| Question | Likely result | Would be surprising | Significant if |
|---|---|---|---|
| Q5 normalization | 20× range narrows; monotonic drop partially survives | Drop completely disappears | Actual variance still falls faster than local GUE |
| Q2 P/Q split | Partial match to explicit formula structure | Perfect correspondence | Any systematic alignment |
| Q1 filter bank | Stated as corollary of Q2 | — | Q2 confirms |
| Q3 bilateral map | Candidate map formally stated; correspondence plausible | Sharp falsification | Map respects both bilateral structures |
| Q4 Layer 1 | CAILculator distinguishes chi3 from zeta Q2 | Identical transform values | Transform ratio deviates from 1.0 |
| Q4 Layer 2 | Deferred or preliminary | Full character decomposition | Q3 conclusive |

---

## Deliverable: Sharpened AIEX-001 Candidate Statement

Phase 18C closes with an explicit candidate statement of the form:

> The Hilbert-Pólya operator H has a natural representation in sedenion space where:
> 1. Its eigenfunctions are indexed by nontrivial Riemann zeros
> 2. The functional equation symmetry s→1−s is realized by the E8 Weyl reflection s_α4 established in Theorem_1b
> 3. The Canonical Six bilateral zero divisor patterns provide the algebraic basis for the spectral structure, with the (A₁)⁶ root system in the 6D bilateral subspace serving as the natural frame for H

The candidate statement need not be proven. It needs to be **precise**, **falsifiable**, and **grounded in the Phase 16–18B empirical record**.

---

## Files

| File | Type | Purpose |
|---|---|---|
| `rh_phase18c_prep.py` | Python script | Q5 normalization fix + Q2 synthesis + data generation |
| `p18c_results.json` | JSON | Structured results |
| `p18c_zeta_q2_sequence_full.json` | JSON | Full zeta Q2 sequence (9,998 values) |
| `p18c_chi3_q2_sequence.json` | JSON | Full chi3 Q2 sequence (1,891 values) |
| `RH_Phase18C_Results.md` | Markdown | Human-readable findings + AIEX-001 candidate statement |

---

*Chavez AI Labs LLC · Applied Pathological Mathematics*
*"Better math, less suffering"*
